gcloud builds submit


zip -r okramen_backup.zip . \
  -x "*.git*" \
  -x "*.gitignore" \
  -x "*.gcloudignore" \
  -x "*.dockerignore" \
  -x "*__pycache__*" \
  -x "*.pyc" \
  -x ".DS_Store" \
  -x "venv/*" \
  -x "app/content/*" \
  -x "env/*"

python ./script/

python ./script/build_data.py 
gunicorn --bind 0.0.0.0:8080 app:app

git add .
git commit -m "update contents"
git push origin main

git add .
git commit -m "Integrate premium Zen theme with full multi-language and SEO optimizations"
git push origin main
